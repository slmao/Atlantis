# specs/

A spec is a proposal: problem, requirements, proposed design, and an
explicit statement of architectural impact. It is written *before*
implementation, reviewed, and approved by a human before a plan is written
against it.

- Template: [template.md](template.md)
- File naming: `specs/<feature-slug>.md`, matching the eventual
  `plans/<feature-slug>.md` and branch name for traceability.
- Status values: `Draft` → `In Review` → `Approved` (or `Rejected` /
  `Superseded by <link>`).
- If a spec's Architectural Impact section says an ADR is required, the ADR
  must exist (at least as `Proposed`) before the spec can move to
  `Approved`.

Full process: [AGENTS.md](../AGENTS.md).

## Spec Registry

This registry is a status index, maintained alongside the specs
themselves. It is not a substitute for reading the spec/plan/ADR files
it links, and it does not grant approval on its own — see the
maintenance rules at the end of this section. For the broader roadmap
this registry feeds into, see
[docs/project-blueprint.md](../docs/project-blueprint.md).

### A. Existing Specs

| ID | Title | Status | Plan | Implementation | Dependencies / Notes |
|---|---|---|---|---|---|
| 0001 | [Project Foundation](0001-project-foundation.md) | `Approved` | [plans/0001-project-foundation.md](../plans/0001-project-foundation.md) (`Approved / Ready for Implementation`) | **Implemented** — `src/core/`, `tests/core/`, `examples/foundation_demo/` exist and match the spec/plan | ADR-0006–0010 (`Accepted`). No dependencies on other specs. |
| 0002 | [Platform Foundation](0002-platform-foundation.md) | `Approved` | [plans/0002-platform-foundation.md](../plans/0002-platform-foundation.md) (`Approved / Ready for Implementation`, Windows portion) | **Windows portion implemented** — `src/platform/` (Windows), `tests/platform/` (incl. Windows smoke tests), `examples/platform_demo/`. Android and iOS remain architecture-only; no `src/platform/src/android/` or `.../ios/` directory exists | Depends on Spec 0001 (Core). ADR-0005 (amended), ADR-0010–0013 (`Accepted`). |
| 0003 | [RHI and Vulkan Windowed Foundation](0003-rhi-vulkan-windowed-foundation.md) | `Approved` | [plans/0003-rhi-vulkan-windowed-foundation.md](../plans/0003-rhi-vulkan-windowed-foundation.md) (`Approved / Ready for Implementation`) | **Implemented and merged** via [PR #14](https://github.com/slmao/Atlantis/pull/14) — `src/rhi/`, `src/vulkan_backend/`, `tests/rhi/`, `tests/vulkan_backend/`, `examples/rhi_vulkan_demo/` exist and match the spec/plan. This is a non-frame, windowed Vulkan presentation *foundation*: swapchain/surface construction and recreation only — no frame acquire/present loop, no `RenderTarget`, and no rendered output are implemented. Spec 0003 itself did not implement RenderGraph; RenderGraph was subsequently implemented under Spec 0005 and merged via [PR #18](https://github.com/slmao/Atlantis/pull/18) (see the Spec 0005 row below). `src/renderer/` still does not exist. | Depends on Specs 0001, 0002 (Windows). ADR-0001–0005 (amended), ADR-0011, ADR-0013, ADR-0014–0016 (`Accepted`). |
| 0004 | [Context-Efficient Documentation and Code Comment Guidelines](0004-context-efficiency-guidelines.md) | `Approved` | [plans/0004-context-efficiency-guidelines.md](../plans/0004-context-efficiency-guidelines.md) (`Approved / Ready for Implementation`) | **Implemented** — `AGENTS.md` now contains the `## Documentation and code comments` section; merged via PR #11 | Governance/documentation convention only; no ADR required (see Architectural Impact). No runtime architecture impact. No dependencies on other specs. |
| 0005 | [RenderGraph Foundation (GPU-Independent Graph Core)](0005-render-graph-foundation.md) | `Approved` | [plans/0005-render-graph-foundation.md](../plans/0005-render-graph-foundation.md) (`Approved / Ready for Implementation`) | **Implemented and merged** via [PR #18](https://github.com/slmao/Atlantis/pull/18) — `src/render_graph/`, `tests/render_graph/` exist and match the spec/plan. This is a GPU-independent RenderGraph *construction/compilation* foundation: pass/resource declaration, single-producer dependency derivation, and deterministic compilation only — no RHI resource binding, command recording, GPU execution, barriers, pass culling, or resource lifetime/aliasing are implemented, and `src/renderer/` still does not exist. | Depends on Spec 0003 (RHI/Vulkan windowed foundation) — implemented, dependency satisfied. [ADR-0017](../adr/0017-render-graph-construction-compile-layering.md) and [ADR-0018](../adr/0018-render-graph-dependency-derivation-and-ordering.md) both `Accepted` alongside this spec's Human Review Approval, recorded 2026-08-09 (see the spec's own Human Review Approval note — all 16 architectural decisions accepted). Joint Spec 0005 + Plan 0005 Human Review completed 2026-08-09, accepting all 19 Plan-stage details in the Plan's own Section 7 with zero Human Review blockers. |
| 0006 | [RHI / RenderGraph Frame Execution Foundation](0006-rhi-render-graph-frame-execution-foundation.md) | `Approved` | [plans/0006-rhi-render-graph-frame-execution-foundation.md](../plans/0006-rhi-render-graph-frame-execution-foundation.md) (`Approved / Ready for Implementation`) | **Implemented and merged** via [PR #23](https://github.com/slmao/Atlantis/pull/23) (implementation) and [PR #24](https://github.com/slmao/Atlantis/pull/24) (post-merge GPU-verification fixes) — `src/rhi/`, `src/vulkan_backend/`, `src/render_graph/`'s execution extension, `tests/rhi/`, `tests/vulkan_backend/`, `tests/render_graph/`, and `examples/frame_execution_demo/` exist and match the spec/plan. Implements the frame-scoped `RenderTarget`, `Presentation::acquireNextTarget()`/`present()` (covering zero-extent, resize, and out-of-date/suboptimal handling), a minimal RHI `CommandList` (`transitionResource`, `clearColor`) and `Device::submit()` on a single-frame-in-flight baseline, and RenderGraph `execute()` with its two guard checks and barrier/transition responsibility. PR #23's implementation was merged before it could be verified on real GPU hardware; PR #24 is the deferred GPU verification (fresh Debug/Release builds, 138/138 GPU-independent tests, 2/2 GPU-required tests, and an interactive `frame_execution_demo` run covering resize/minimize/restore/close, all with Validation Layers clean) and fixed three real Vulkan Validation Layer defects it found (missing `VK_IMAGE_USAGE_TRANSFER_DST_BIT` on swapchain images; an acquire-complete semaphore reuse race; a render-finished semaphore reuse race across `vkQueuePresentKHR`) plus a reproducible crash (resize immediately followed by minimize) — none required a public API, ownership, synchronization-model, module-boundary, or ADR change. | Depends on Spec 0003 (RHI/Vulkan windowed foundation, `Approved`/implemented) and Spec 0005 (RenderGraph Foundation, `Approved`/implemented) — both dependencies satisfied. [ADR-0019](../adr/0019-presentation-acquire-present-and-rendertarget-frame-borrow-contract.md), [ADR-0020](../adr/0020-rhi-minimal-resource-command-recording-and-submission-interface.md), and [ADR-0021](../adr/0021-render-graph-rhi-execution-integration-and-barrier-responsibility.md) all `Accepted` alongside this spec's Human Review Approval, recorded 2026-08-09 (see the spec's own Human Review Approval note). This spec was the frame-execution prerequisite the Candidate Spec Backlog's "Minimal Renderer" entry (Section B below) needed in addition to Spec 0005 — both dependencies are now satisfied. |
| 0007 | [Minimal Renderer](0007-minimal-renderer.md) | `Approved` | [plans/0007-minimal-renderer.md](../plans/0007-minimal-renderer.md) (`Approved / Ready for Implementation`, joint Spec+Plan Human Review recorded 2026-08-11, merged via [PR #27](https://github.com/slmao/Atlantis/pull/27)) | **Implemented and merged.** [PR #28](https://github.com/slmao/Atlantis/pull/28) landed the original implementation: `src/renderer/` (`Mesh`/`Material`/`DrawItem`/`Renderer`), RHI `Buffer`/`Texture`/`Pipeline` and the new `ColorAttachmentOutput`/`DepthAttachmentReadWrite` draw-command surface, RenderGraph multi-attachment/draw-pass execution, the Vulkan dynamic-rendering dual path, and `examples/minimal_renderer_demo/`. Post-merge review found the shipped dynamic-rendering Core path deviated from [ADR-0024](../adr/0024-vulkan-dynamic-rendering-for-attachments.md)'s approved design; [PR #29](https://github.com/slmao/Atlantis/pull/29) is the documentation-only Human Review that accepted an amendment fixing that deviation (a loader-version-gated instance `apiVersion` strategy — request Vulkan 1.3 only when the loader itself supports it, never raising the device-level minimum), and [PR #30](https://github.com/slmao/Atlantis/pull/30) is the code fix implementing that amendment, separating the Vulkan Core and Extension dynamic-rendering paths so the Core path no longer requests, enables, or resolves through `VK_KHR_dynamic_rendering`. **Scope delivered:** a real, visible, depth-tested mesh (fixed cube, per-vertex color) drawn through `Renderer` → RenderGraph → RHI → Vulkan Backend on Windows, with a camera uniform buffer, per-draw-item push-constant transforms, and a caller-owned resize/attachment-format-change lifecycle. **Dynamic Rendering, final state:** the Vulkan Backend's instance requests Vulkan 1.3 if and only if the loader itself reports support for it (queried before `vkCreateInstance()`); the Core path (used when the loader and the selected physical device both qualify) has no dependency on `VK_KHR_dynamic_rendering` or its extension-dependency chain; the Extension path remains available, unchanged, for devices below Vulkan 1.3 that advertise `VK_KHR_dynamic_rendering` — preserving the Windows/Android device-compatibility range [ADR-0024](../adr/0024-vulkan-dynamic-rendering-for-attachments.md) was written to serve, with no narrowing of AGENTS.md's future Android target-platform commitment. See ADR-0024's "Accepted Amendment — 2026-08-13" section for the full design and its verification plan. **Verified:** Debug/Release builds and the full GPU-independent + GPU-required test suites pass (PR #30: 190/190 and 189/189 GPU-independent, 4/4 GPU-required on both configs); the Core dynamic-rendering path is confirmed working on real hardware (Intel Arc B370, Windows), Vulkan Validation Layers clean throughout. The Extension dynamic-rendering path has no real-GPU coverage in this environment — verified only by GPU-independent unit tests and code review, per PR #30's own explicit disclosure. Format-change (swapchain format changing at runtime) handling is verified by code inspection only — no second monitor was available to genuinely trigger it. See [PR #28](https://github.com/slmao/Atlantis/pull/28) and [PR #30](https://github.com/slmao/Atlantis/pull/30) for full verification detail and known limitations. **Not implemented** (per Spec 0007's own Non-Goals): Shader System, Runtime (the module), Android, iOS, headless rendering, image regression testing, a scene graph/ECS/asset system or model loader, multiple materials/lighting/shadows/texturing, GPU-driven/bindless/instanced/indirect draws, hot-reload, multiple frames in flight, multi-threading, a general GPU memory suballocator, a second graphics backend, a general descriptor-set/bindless system, and cross-owner shared ownership of `Mesh`/`Material`/GPU resources. | Depends on Spec 0005 (RenderGraph Foundation) and Spec 0006 (RHI/RenderGraph Frame Execution Foundation) — both `Approved`/implemented. [ADR-0022](../adr/0022-minimal-renderer-public-api-and-resource-ownership.md)–[ADR-0027](../adr/0027-temporary-precompiled-spirv-shader-artifacts.md) all `Accepted`, alongside this spec's Human Review Approval recorded 2026-08-11; ADR-0024 additionally carries an Accepted Amendment recorded 2026-08-13 (see PR #29/#30 above). |
| 0008 | [Shader System Foundation](0008-shader-system-foundation.md) | `Approved` | [plans/0008-shader-system-foundation.md](../plans/0008-shader-system-foundation.md) (`Approved / Ready for Implementation`, Human Review recorded 2026-08-15, approval record merged into `main` via [PR #35](https://github.com/slmao/Atlantis/pull/35)) | **Implemented and merged** via [PR #36](https://github.com/slmao/Atlantis/pull/36) — `src/shader_system/` (Atlantis Shader System: a private JSON parser, the `ReflectionMetadata` schema and loader/saver, `transformSlangReflectionJson()`, the fixed Minimal Renderer descriptor contract and its validator, `slangc`/`spirv-val` argv builders), `src/shader_system/rhi_integration/` (`Atlantis::ShaderSystemRhiIntegration`, the only target depending on both `Atlantis::ShaderSystem` and `Atlantis::RHI`), `src/tools/shader_compiler/` (Atlantis Tools' first real content — a `CreateProcessW`-based process launcher and `compile_and_validate.cpp` implementing the full compile/reflect/validate/publish pipeline), and the CMake `atlantis_add_slang_shader_pair()` stamp-based producer exist and match the spec/plan. `shaders/minimal_renderer/minimal_mesh.slang` replaces ADR-0027's checked-in GLSL/SPIR-V pair; no `.spv`, `.glsl`, or generated reflection JSON is checked into the repository any longer. **Deviations from Plan** (all mechanical, no architectural effect, disclosed in the PR): `Result<std::monostate, E>` in place of `Result<void, E>` (Core's `Result<T,E>` cannot hold `void`, matching `Device::waitIdle()`'s own precedent); the process-execution logic is factored into a private `atlantis_shader_compiler_lib` for testability; descriptor-contract/push-constant validation is scoped per shader stage, matching real captured Slang reflection evidence (a real fragment-stage reflection correctly has zero descriptor bindings); a second, explicit CMake `PARENT_SCOPE` propagation (a single `PARENT_SCOPE` only reaches the immediate calling directory, not sibling consumers — confirmed empirically); and a documented, human-reviewed one-time shader recompile the first time a second CMake configuration builds in an existing binary tree (does not affect ADR-0031's single-shared-artifact correctness). **Verified:** independent, fresh Debug and Release builds; the full GPU-independent, `tool`-labeled (real `slangc`/`spirv-val`), and GPU-required test suites all pass (Intel Arc B370, Windows), Vulkan Validation Layers clean throughout; a manual demo run confirmed (with screenshot evidence) a correctly-shaded, centered mesh, with resize/minimize/restore/close all verified. PR #36 also received an independent, evidence-driven read-only code review with no correctness or architectural blockers found; a small number of non-blocking observations were accepted as known limitations, none requiring a code change — an unreachable `StageMismatch` validation branch in the current call path, and that manually deleting a single generated shader artifact (or running a `clean`) without touching the build-tree stamp is not detected by an ordinary incremental rebuild (an inherent consequence of CMake's OUTPUT/BYPRODUCTS model, not a defect). **Not implemented** (per Spec 0008's own Non-Goals, unchanged): runtime/hot-reload shader compilation, a general descriptor-set/bindless system, shader debug-info, and any change to `Atlantis::RHI`'s public API or the Vulkan Backend's fixed descriptor layout. | Depends on Spec 0007 (Minimal Renderer) — `Approved`, implemented, providing this spec's first real consumer. [ADR-0028](../adr/0028-shader-system-source-language-and-compiler.md)–[ADR-0031](../adr/0031-shader-system-artifact-versioning-and-reproducibility.md), all `Accepted`, alongside this spec's Human Review Approval recorded 2026-08-14 (Vulkan 1.0 / SPIR-V 1.0 compatibility baseline with mandatory `spirv-val`, and precise `E50011` warning suppression, both confirmed as part of that approval). Supersedes ADR-0027's *mechanism*, now implemented; ADR-0027 itself remains `Accepted` and unmodified. |
| 0009 | [Long-Term Engine Architecture Alignment](0009-long-term-engine-architecture-alignment.md) | `Approved` | [plans/0009-long-term-engine-architecture-alignment.md](../plans/0009-long-term-engine-architecture-alignment.md) (`Approved` / Ready for Implementation, Human Review recorded 2026-08-15) — scoped to a future `docs/architecture/engine_architecture.md` overview document and minimal, non-duplicative navigation-link updates to a small number of existing documents (including `README.md`, confirmed in scope by that Plan's own Human Review Approval). | Not started — this Spec is a governance/direction document only; it introduces no code, no module, and authorizes no Implementation. It sorts a human-provided external architecture draft into what Atlantis can adopt now as long-term principle, what remains a compatible future direction, what needs its own future subsystem Spec (Runtime Host, World/ECS, Asset, Tool/Editor Protocol, and other existing Candidate Backlog items below), and what conflicts with `Accepted`/`Approved` Phase 1 decisions (Linux as a target platform, immediate second-graphics-backend implementation/scaffolding, Shader System multi-target/hot-reload, an immediate Job System) and is therefore rejected or deferred, not adopted; Direct3D 12/Metal are additionally named as long-term candidate sibling Device Backends (implementation still deferred, no Phase 1 scaffolding) per ADR-0037. | Depends on Spec 0007 (Minimal Renderer) and Spec 0008 (Shader System Foundation) — both `Approved`, implemented, as the existing foundation this alignment work builds on. [ADR-0032](../adr/0032-conceptual-architecture-layers-versus-source-module-ownership.md)–[ADR-0037](../adr/0037-long-term-device-backend-extensibility-without-phase1-scaffolding.md), all `Accepted` alongside this spec's Human Review Approval recorded 2026-08-15. Does not modify Spec 0008/ADR-0028–0031, `AGENTS.md`, `docs/architecture/module_boundaries.md`, or `docs/project-blueprint.md`. |
| 0010 | [Headless Rendering Foundation](0010-headless-rendering-foundation.md) | `Approved` | [plans/0010-headless-rendering-foundation.md](../plans/0010-headless-rendering-foundation.md) (`Approved`, Human Review Approval recorded 2026-08-16 following two independent Plan Review rounds — see the plan's own Human Review Approval note, [PR #45](https://github.com/slmao/Atlantis/pull/45), [PR #46](https://github.com/slmao/Atlantis/pull/46) — including corrections for a missed private-access call site, an exception-throwing `dynamic_cast` form, and a stale dependency claim) | **Implemented and merged** via [PR #48](https://github.com/slmao/Atlantis/pull/48). Implements this Spec's design in full, strictly against Plan 0010's Step 1–6 sequencing: a new `atlantis::rhi::OffscreenTarget` interface, `ResourceState::TransferSource`/`BufferPurpose::Readback`/`CommandList::copyRenderTargetToBuffer()` for GPU-to-CPU readback, `ResourceBinding`'s `incomingState`/`finalState` generalization of `render_graph::execute()`, `Renderer::drawFrame()`'s new required `finalColorState` parameter, and the Vulkan Backend's `VulkanOffscreenTarget`/`VulkanOffscreenRenderTarget` plus a shared private `VulkanRenderTargetAccess` boundary adopted via checked pointer-form `dynamic_cast` at all five affected call sites (never a thrown exception). New `examples/headless_rendering_demo` and `tests/vulkan_backend/headless_rendering_gpu_tests.cpp`. **Verified** (no CI pipeline exists yet; verification below was run locally, per PR #48's own final "Testing performed" and "Manual verification record"): Debug and Release builds clean; GPU-independent tests 244/244 Debug, 243/243 Release; GPU-required tests 12/12 both configs on real hardware (Intel Arc B370, integrated GPU, Vulkan 1.4.335) — 4 pre-existing windowed regression cases plus 8 new headless cases, Vulkan Validation Layers clean throughout; `examples/headless_rendering_demo` runs to completion with its own basic content check passing on all 3 cycles, both configs; **interactive windowed regression** (`frame_execution_demo`/`minimal_renderer_demo`, Debug and Release — window/visible frame, continuous resize, minimize/restore, mid-frame exit, clean exit) performed and observed directly by a human verifier, all 4 configuration/demo combinations PASS, Validation Layers clean. Four files outside the Plan's own enumerated list needed the same mechanical `finalState`/`drawFrame()` fix the Plan's own three sites already required (disclosed in PR #48's own "Deviations from Plan" section) — no design implication. **Disclosed limitation:** all real-hardware verification above ran on a single GPU vendor/driver (Intel Arc, integrated) — no second GPU vendor or driver combination was available in this environment; this is not cross-vendor coverage. Golden-image comparison, tolerance methodology, and CI image-regression gating remain explicitly out of this Spec's scope — see its own Non-Goals; Image Regression Testing remains a separate, not-yet-specced candidate (see Section B below). | Depends on Spec 0007 (Minimal Renderer) — `Approved`, implemented. [ADR-0038](../adr/0038-headless-offscreen-rendertarget-construction-and-ownership.md)–[ADR-0040](../adr/0040-gpu-to-cpu-readback-rhi-capability.md), all `Accepted`, plus an **Accepted Amendment** to [ADR-0022](../adr/0022-minimal-renderer-public-api-and-resource-ownership.md) (`Renderer::drawFrame()`'s new `finalColorState` parameter) — all accepted alongside this spec's Human Review Approval recorded 2026-08-16. Promoted from Candidate Backlog Section B below (formerly "Headless Rendering," Candidate Order 2). |

No spec above is marked `Complete` — this registry only uses the status
values `specs/README.md` and `plans/README.md` already define
(`Draft` / `In Review` / `Approved` / `Rejected` / `Superseded`, and for
plans additionally `Approved / Ready for Implementation`). Where a
spec's Plan records an explicit Human Review approval note (Specs 0001,
0002, and 0003 do, with dates, in their own Plan files), that is the
authoritative record — this table does not restate it, only points to
it. Plan 0003 records a joint Spec + Plan Human Review completed
2026-08-08; Spec 0003 itself remains `Approved` (an approved spec does
not change status when its plan is implemented). This table's
Implementation column records the fact that PR #14 merged Spec 0003's
implementation into `main`; the registry itself grants no new approval
and is not a substitute for reading the Plan or PR.

### B. Candidate Spec Backlog

**Not an approved commitment.** This is a dependency-ordered list of
candidate future work, for planning visibility only. No entry here has
a Spec written, and none is authorized for implementation — see
[AGENTS.md](../AGENTS.md) and
[docs/project-blueprint.md](../docs/project-blueprint.md) Section 1.
No formal spec number is pre-assigned to any entry; numbers are
assigned only when a real spec is drafted.

**Confirmed working priority order (human-directed 2026-08-15, confirmed
2026-08-16 alongside Spec 0010's Human Review Approval):** Headless
Rendering Foundation (Spec 0010, `Approved` — Section A above,
**implemented and merged via PR #48**) → Image Regression Testing →
Android Platform and Vulkan Presentation. Nothing about Android
Platform's own dependencies or scope changes — it still depends only on
Spec 0002/Spec 0003, both implemented, and remains independent of
Headless Rendering/Image Regression Testing in either direction. **This
was a confirmed ordering for which candidate gets specced next, not an
approval of either candidate's own scope or an authorization to
implement either** — with Headless Rendering now implemented, Image
Regression Testing is next in this order; neither it nor Android
Platform has a Spec drafted yet, and both remain `Candidate` below
(status changes for a candidate itself come only from a real Spec, Plan,
or merged PR, never from this ordering note). This ordering note does
not authorize or deprioritize implementation of anything listed, and
does not itself promote Image
Regression Testing past `Candidate`.

| Candidate Order | Working Title | Depends On | Intended Outcome | Governance State |
|---|---|---|---|---|
| 1 | Image Regression Testing | Spec 0010 (Headless Rendering Foundation) — `Approved`, implemented and merged via [PR #48](https://github.com/slmao/Atlantis/pull/48) | Golden-image comparison, tolerance methodology, CI gating | Candidate — spec not yet drafted |
| 2 | Android Platform and Vulkan Presentation | Spec 0002 (Platform interface), Spec 0003 (RHI/Vulkan Backend construction boundary) | Android Activity/Surface lifecycle, `ANativeWindow` borrowed semantics, `VK_KHR_android_surface` WSI, reusing the existing RHI/RenderGraph/Renderer stack unchanged | Candidate — spec not yet drafted |
| 3 | Asset System Foundation | Spec 0007 (Minimal Renderer) — `Approved`, implemented, for real asset consumers | Asset GUID/metadata model, authoring-to-runtime conversion boundary | Candidate — spec not yet drafted |
| 4 | Runtime Host and Composition Root | Spec 0002 (Platform), Spec 0003 (RHI), Spec 0005 (RenderGraph Foundation), Spec 0007 (Minimal Renderer) | The actual `Atlantis Runtime` module — distinct from Spec 0003's own non-shipping verification composition, which is explicitly not a preview of Runtime | Candidate — spec not yet drafted |
| 5 | World/ECS Foundation | Candidate 4 (Runtime) | Authoritative world/entity state model — no ECS library or design chosen yet | Candidate — spec not yet drafted; ECS choice not made |
| 6 | Serialization and Stable Identity | Candidate 5 (World/ECS) | Stable GUID/handle schemes, schema versioning and migration | Candidate — spec not yet drafted |
| 7 | Tool/Editor Connection Protocol | Candidate 4 (Runtime) | Editor↔Runtime communication boundary — in-process vs. multi-process model not chosen | Candidate — spec not yet drafted |
| 8 | Gameplay SDK | Candidate 5 (World/ECS), Candidate 7 (Tool/Editor protocol) | First-party gameplay authoring surface — gameplay language not chosen | Candidate — spec not yet drafted |
| 9 | Research/Simulation API | Candidate 4 (Runtime), Spec 0010 (Headless Rendering Foundation) | Observation/Action/Episode-style research interface — language/transport not chosen | Candidate — spec not yet drafted |
| 10 | AI Inference Integration | Candidate 4 (Runtime) | Pluggable inference backend for gameplay/tools use — must not shape Phase 1 rendering abstractions; inference backend not chosen | Candidate — spec not yet drafted; future AI workload, does not shape Phase 1 |
| 11 | UGC Sandbox and Package Model | Candidate 8 (Gameplay SDK) | Sandboxed, capability-scoped user content execution — UGC VM/language not chosen | Candidate — spec not yet drafted; future UGC workload, does not shape Phase 1 |

Items 10–11 (and any future neural-rendering, GPU-driven-rendering, or
world-model candidate) are future AI/UGC/neural workloads: per
[AGENTS.md](../AGENTS.md), they are future phases and must not shape
Phase 1 abstractions ahead of their own approved spec.

### Backlog maintenance rules

- When a candidate item gets a real Spec drafted, assign it a formal
  spec number at that point and move/replace its row into Section A —
  do not pre-assign a number while it is still a backlog entry.
- Status changes in either table come only from a real Spec, Plan, or
  merged PR — never from editing this registry's prose alone.
- This registry is not a Human Review approval and does not authorize
  implementation of anything listed in it.
- An Accepted ADR or an Approved Spec always takes precedence over
  anything stated in this registry.
