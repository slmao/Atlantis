# specs/

A spec is a proposal: problem, requirements, proposed design, and an
explicit statement of architectural impact. It is written *before*
implementation, reviewed, and approved by a human before a plan is written
against it.

- Template: [template.md](template.md)
- File naming: `specs/<feature-slug>.md`, matching the eventual
  `plans/<feature-slug>.md` and branch name for traceability.
- Status values: `Draft` → `In Review` → `Approved` (or `Rejected` /
  `Superseded by <link>`).
- If a spec's Architectural Impact section says an ADR is required, the ADR
  must exist (at least as `Proposed`) before the spec can move to
  `Approved`.

Full process: [AGENTS.md](../AGENTS.md).

## Spec Registry

This registry is a status index, maintained alongside the specs
themselves. It is not a substitute for reading the spec/plan/ADR files
it links, and it does not grant approval on its own — see the
maintenance rules at the end of this section. For the broader roadmap
this registry feeds into, see
[docs/project-blueprint.md](../docs/project-blueprint.md).

### A. Existing Specs

| ID | Title | Status | Plan | Implementation | Dependencies / Notes |
|---|---|---|---|---|---|
| 0001 | [Project Foundation](0001-project-foundation.md) | `Approved` | [plans/0001-project-foundation.md](../plans/0001-project-foundation.md) (`Approved / Ready for Implementation`) | **Implemented** — `src/core/`, `tests/core/`, `examples/foundation_demo/` exist and match the spec/plan | ADR-0006–0010 (`Accepted`). No dependencies on other specs. |
| 0002 | [Platform Foundation](0002-platform-foundation.md) | `Approved` | [plans/0002-platform-foundation.md](../plans/0002-platform-foundation.md) (`Approved / Ready for Implementation`, Windows portion) | **Windows portion implemented** — `src/platform/` (Windows), `tests/platform/` (incl. Windows smoke tests), `examples/platform_demo/`. Android and iOS remain architecture-only; no `src/platform/src/android/` or `.../ios/` directory exists | Depends on Spec 0001 (Core). ADR-0005 (amended), ADR-0010–0013 (`Accepted`). |
| 0003 | [RHI and Vulkan Windowed Foundation](0003-rhi-vulkan-windowed-foundation.md) | `Approved` | [plans/0003-rhi-vulkan-windowed-foundation.md](../plans/0003-rhi-vulkan-windowed-foundation.md) (`Approved / Ready for Implementation`) | **Implemented and merged** via [PR #14](https://github.com/slmao/Atlantis/pull/14) — `src/rhi/`, `src/vulkan_backend/`, `tests/rhi/`, `tests/vulkan_backend/`, `examples/rhi_vulkan_demo/` exist and match the spec/plan. This is a non-frame, windowed Vulkan presentation *foundation*: swapchain/surface construction and recreation only — no frame acquire/present loop, no `RenderTarget`, and no rendered output are implemented. Spec 0003 itself did not implement RenderGraph; RenderGraph was subsequently implemented under Spec 0005 and merged via [PR #18](https://github.com/slmao/Atlantis/pull/18) (see the Spec 0005 row below). `src/renderer/` still does not exist. | Depends on Specs 0001, 0002 (Windows). ADR-0001–0005 (amended), ADR-0011, ADR-0013, ADR-0014–0016 (`Accepted`). |
| 0004 | [Context-Efficient Documentation and Code Comment Guidelines](0004-context-efficiency-guidelines.md) | `Approved` | [plans/0004-context-efficiency-guidelines.md](../plans/0004-context-efficiency-guidelines.md) (`Approved / Ready for Implementation`) | **Implemented** — `AGENTS.md` now contains the `## Documentation and code comments` section; merged via PR #11 | Governance/documentation convention only; no ADR required (see Architectural Impact). No runtime architecture impact. No dependencies on other specs. |
| 0005 | [RenderGraph Foundation (GPU-Independent Graph Core)](0005-render-graph-foundation.md) | `Approved` | [plans/0005-render-graph-foundation.md](../plans/0005-render-graph-foundation.md) (`Approved / Ready for Implementation`) | **Implemented and merged** via [PR #18](https://github.com/slmao/Atlantis/pull/18) — `src/render_graph/`, `tests/render_graph/` exist and match the spec/plan. This is a GPU-independent RenderGraph *construction/compilation* foundation: pass/resource declaration, single-producer dependency derivation, and deterministic compilation only — no RHI resource binding, command recording, GPU execution, barriers, pass culling, or resource lifetime/aliasing are implemented, and `src/renderer/` still does not exist. | Depends on Spec 0003 (RHI/Vulkan windowed foundation) — implemented, dependency satisfied. [ADR-0017](../adr/0017-render-graph-construction-compile-layering.md) and [ADR-0018](../adr/0018-render-graph-dependency-derivation-and-ordering.md) both `Accepted` alongside this spec's Human Review Approval, recorded 2026-08-09 (see the spec's own Human Review Approval note — all 16 architectural decisions accepted). Joint Spec 0005 + Plan 0005 Human Review completed 2026-08-09, accepting all 19 Plan-stage details in the Plan's own Section 7 with zero Human Review blockers. |
| 0006 | [RHI / RenderGraph Frame Execution Foundation](0006-rhi-render-graph-frame-execution-foundation.md) | `Approved` | [plans/0006-rhi-render-graph-frame-execution-foundation.md](../plans/0006-rhi-render-graph-frame-execution-foundation.md) (`Approved / Ready for Implementation`) | **Implemented and merged** via [PR #23](https://github.com/slmao/Atlantis/pull/23) (implementation) and [PR #24](https://github.com/slmao/Atlantis/pull/24) (post-merge GPU-verification fixes) — `src/rhi/`, `src/vulkan_backend/`, `src/render_graph/`'s execution extension, `tests/rhi/`, `tests/vulkan_backend/`, `tests/render_graph/`, and `examples/frame_execution_demo/` exist and match the spec/plan. Implements the frame-scoped `RenderTarget`, `Presentation::acquireNextTarget()`/`present()` (covering zero-extent, resize, and out-of-date/suboptimal handling), a minimal RHI `CommandList` (`transitionResource`, `clearColor`) and `Device::submit()` on a single-frame-in-flight baseline, and RenderGraph `execute()` with its two guard checks and barrier/transition responsibility. PR #23's implementation was merged before it could be verified on real GPU hardware; PR #24 is the deferred GPU verification (fresh Debug/Release builds, 138/138 GPU-independent tests, 2/2 GPU-required tests, and an interactive `frame_execution_demo` run covering resize/minimize/restore/close, all with Validation Layers clean) and fixed three real Vulkan Validation Layer defects it found (missing `VK_IMAGE_USAGE_TRANSFER_DST_BIT` on swapchain images; an acquire-complete semaphore reuse race; a render-finished semaphore reuse race across `vkQueuePresentKHR`) plus a reproducible crash (resize immediately followed by minimize) — none required a public API, ownership, synchronization-model, module-boundary, or ADR change. | Depends on Spec 0003 (RHI/Vulkan windowed foundation, `Approved`/implemented) and Spec 0005 (RenderGraph Foundation, `Approved`/implemented) — both dependencies satisfied. [ADR-0019](../adr/0019-presentation-acquire-present-and-rendertarget-frame-borrow-contract.md), [ADR-0020](../adr/0020-rhi-minimal-resource-command-recording-and-submission-interface.md), and [ADR-0021](../adr/0021-render-graph-rhi-execution-integration-and-barrier-responsibility.md) all `Accepted` alongside this spec's Human Review Approval, recorded 2026-08-09 (see the spec's own Human Review Approval note). This spec was the frame-execution prerequisite the Candidate Spec Backlog's "Minimal Renderer" entry (Section B below) needed in addition to Spec 0005 — both dependencies are now satisfied. |
| 0007 | [Minimal Renderer](0007-minimal-renderer.md) | `Approved` | [plans/0007-minimal-renderer.md](../plans/0007-minimal-renderer.md) (`Approved / Ready for Implementation`, joint Spec+Plan Human Review recorded 2026-08-11, merged via [PR #27](https://github.com/slmao/Atlantis/pull/27)) | **Implemented and merged.** [PR #28](https://github.com/slmao/Atlantis/pull/28) landed the original implementation: `src/renderer/` (`Mesh`/`Material`/`DrawItem`/`Renderer`), RHI `Buffer`/`Texture`/`Pipeline` and the new `ColorAttachmentOutput`/`DepthAttachmentReadWrite` draw-command surface, RenderGraph multi-attachment/draw-pass execution, the Vulkan dynamic-rendering dual path, and `examples/minimal_renderer_demo/`. Post-merge review found the shipped dynamic-rendering Core path deviated from [ADR-0024](../adr/0024-vulkan-dynamic-rendering-for-attachments.md)'s approved design; [PR #29](https://github.com/slmao/Atlantis/pull/29) is the documentation-only Human Review that accepted an amendment fixing that deviation (a loader-version-gated instance `apiVersion` strategy — request Vulkan 1.3 only when the loader itself supports it, never raising the device-level minimum), and [PR #30](https://github.com/slmao/Atlantis/pull/30) is the code fix implementing that amendment, separating the Vulkan Core and Extension dynamic-rendering paths so the Core path no longer requests, enables, or resolves through `VK_KHR_dynamic_rendering`. **Scope delivered:** a real, visible, depth-tested mesh (fixed cube, per-vertex color) drawn through `Renderer` → RenderGraph → RHI → Vulkan Backend on Windows, with a camera uniform buffer, per-draw-item push-constant transforms, and a caller-owned resize/attachment-format-change lifecycle. **Dynamic Rendering, final state:** the Vulkan Backend's instance requests Vulkan 1.3 if and only if the loader itself reports support for it (queried before `vkCreateInstance()`); the Core path (used when the loader and the selected physical device both qualify) has no dependency on `VK_KHR_dynamic_rendering` or its extension-dependency chain; the Extension path remains available, unchanged, for devices below Vulkan 1.3 that advertise `VK_KHR_dynamic_rendering` — preserving the Windows/Android device-compatibility range [ADR-0024](../adr/0024-vulkan-dynamic-rendering-for-attachments.md) was written to serve, with no narrowing of AGENTS.md's future Android target-platform commitment. See ADR-0024's "Accepted Amendment — 2026-08-13" section for the full design and its verification plan. **Verified:** Debug/Release builds and the full GPU-independent + GPU-required test suites pass (PR #30: 190/190 and 189/189 GPU-independent, 4/4 GPU-required on both configs); the Core dynamic-rendering path is confirmed working on real hardware (Intel Arc B370, Windows), Vulkan Validation Layers clean throughout. The Extension dynamic-rendering path has no real-GPU coverage in this environment — verified only by GPU-independent unit tests and code review, per PR #30's own explicit disclosure. Format-change (swapchain format changing at runtime) handling is verified by code inspection only — no second monitor was available to genuinely trigger it. See [PR #28](https://github.com/slmao/Atlantis/pull/28) and [PR #30](https://github.com/slmao/Atlantis/pull/30) for full verification detail and known limitations. **Not implemented** (per Spec 0007's own Non-Goals): Shader System, Runtime (the module), Android, iOS, headless rendering, image regression testing, a scene graph/ECS/asset system or model loader, multiple materials/lighting/shadows/texturing, GPU-driven/bindless/instanced/indirect draws, hot-reload, multiple frames in flight, multi-threading, a general GPU memory suballocator, a second graphics backend, a general descriptor-set/bindless system, and cross-owner shared ownership of `Mesh`/`Material`/GPU resources. | Depends on Spec 0005 (RenderGraph Foundation) and Spec 0006 (RHI/RenderGraph Frame Execution Foundation) — both `Approved`/implemented. [ADR-0022](../adr/0022-minimal-renderer-public-api-and-resource-ownership.md)–[ADR-0027](../adr/0027-temporary-precompiled-spirv-shader-artifacts.md) all `Accepted`, alongside this spec's Human Review Approval recorded 2026-08-11; ADR-0024 additionally carries an Accepted Amendment recorded 2026-08-13 (see PR #29/#30 above). |
| 0008 | [Shader System Foundation](0008-shader-system-foundation.md) | `Approved` | [plans/0008-shader-system-foundation.md](../plans/0008-shader-system-foundation.md) (`Approved / Ready for Implementation`, Human Review recorded 2026-08-15, approval record merged into `main` via [PR #35](https://github.com/slmao/Atlantis/pull/35)) | **Implemented and merged** via [PR #36](https://github.com/slmao/Atlantis/pull/36) — `src/shader_system/` (Atlantis Shader System: a private JSON parser, the `ReflectionMetadata` schema and loader/saver, `transformSlangReflectionJson()`, the fixed Minimal Renderer descriptor contract and its validator, `slangc`/`spirv-val` argv builders), `src/shader_system/rhi_integration/` (`Atlantis::ShaderSystemRhiIntegration`, the only target depending on both `Atlantis::ShaderSystem` and `Atlantis::RHI`), `src/tools/shader_compiler/` (Atlantis Tools' first real content — a `CreateProcessW`-based process launcher and `compile_and_validate.cpp` implementing the full compile/reflect/validate/publish pipeline), and the CMake `atlantis_add_slang_shader_pair()` stamp-based producer exist and match the spec/plan. `shaders/minimal_renderer/minimal_mesh.slang` replaces ADR-0027's checked-in GLSL/SPIR-V pair; no `.spv`, `.glsl`, or generated reflection JSON is checked into the repository any longer. **Deviations from Plan** (all mechanical, no architectural effect, disclosed in the PR): `Result<std::monostate, E>` in place of `Result<void, E>` (Core's `Result<T,E>` cannot hold `void`, matching `Device::waitIdle()`'s own precedent); the process-execution logic is factored into a private `atlantis_shader_compiler_lib` for testability; descriptor-contract/push-constant validation is scoped per shader stage, matching real captured Slang reflection evidence (a real fragment-stage reflection correctly has zero descriptor bindings); a second, explicit CMake `PARENT_SCOPE` propagation (a single `PARENT_SCOPE` only reaches the immediate calling directory, not sibling consumers — confirmed empirically); and a documented, human-reviewed one-time shader recompile the first time a second CMake configuration builds in an existing binary tree (does not affect ADR-0031's single-shared-artifact correctness). **Verified:** independent, fresh Debug and Release builds; the full GPU-independent, `tool`-labeled (real `slangc`/`spirv-val`), and GPU-required test suites all pass (Intel Arc B370, Windows), Vulkan Validation Layers clean throughout; a manual demo run confirmed (with screenshot evidence) a correctly-shaded, centered mesh, with resize/minimize/restore/close all verified. PR #36 also received an independent, evidence-driven read-only code review with no correctness or architectural blockers found; a small number of non-blocking observations were accepted as known limitations, none requiring a code change — an unreachable `StageMismatch` validation branch in the current call path, and that manually deleting a single generated shader artifact (or running a `clean`) without touching the build-tree stamp is not detected by an ordinary incremental rebuild (an inherent consequence of CMake's OUTPUT/BYPRODUCTS model, not a defect). **Not implemented** (per Spec 0008's own Non-Goals, unchanged): runtime/hot-reload shader compilation, a general descriptor-set/bindless system, shader debug-info, and any change to `Atlantis::RHI`'s public API or the Vulkan Backend's fixed descriptor layout. | Depends on Spec 0007 (Minimal Renderer) — `Approved`, implemented, providing this spec's first real consumer. [ADR-0028](../adr/0028-shader-system-source-language-and-compiler.md)–[ADR-0031](../adr/0031-shader-system-artifact-versioning-and-reproducibility.md), all `Accepted`, alongside this spec's Human Review Approval recorded 2026-08-14 (Vulkan 1.0 / SPIR-V 1.0 compatibility baseline with mandatory `spirv-val`, and precise `E50011` warning suppression, both confirmed as part of that approval). Supersedes ADR-0027's *mechanism*, now implemented; ADR-0027 itself remains `Accepted` and unmodified. |
| 0009 | [Long-Term Engine Architecture Alignment](0009-long-term-engine-architecture-alignment.md) | `Approved` | [plans/0009-long-term-engine-architecture-alignment.md](../plans/0009-long-term-engine-architecture-alignment.md) (`Approved` / Ready for Implementation, Human Review recorded 2026-08-15) — scoped to a future `docs/architecture/engine_architecture.md` overview document and minimal, non-duplicative navigation-link updates to a small number of existing documents (including `README.md`, confirmed in scope by that Plan's own Human Review Approval). | Not started — this Spec is a governance/direction document only; it introduces no code, no module, and authorizes no Implementation. It sorts a human-provided external architecture draft into what Atlantis can adopt now as long-term principle, what remains a compatible future direction, what needs its own future subsystem Spec (Runtime Host, World/ECS, Asset, Tool/Editor Protocol, and other existing Candidate Backlog items below), and what conflicts with `Accepted`/`Approved` Phase 1 decisions (Linux as a target platform, immediate second-graphics-backend implementation/scaffolding, Shader System multi-target/hot-reload, an immediate Job System) and is therefore rejected or deferred, not adopted; Direct3D 12/Metal are additionally named as long-term candidate sibling Device Backends (implementation still deferred, no Phase 1 scaffolding) per ADR-0037. | Depends on Spec 0007 (Minimal Renderer) and Spec 0008 (Shader System Foundation) — both `Approved`, implemented, as the existing foundation this alignment work builds on. [ADR-0032](../adr/0032-conceptual-architecture-layers-versus-source-module-ownership.md)–[ADR-0037](../adr/0037-long-term-device-backend-extensibility-without-phase1-scaffolding.md), all `Accepted` alongside this spec's Human Review Approval recorded 2026-08-15. Does not modify Spec 0008/ADR-0028–0031, `AGENTS.md`, `docs/architecture/module_boundaries.md`, or `docs/project-blueprint.md`. |
| 0010 | [Headless Rendering Foundation](0010-headless-rendering-foundation.md) | `Approved` | [plans/0010-headless-rendering-foundation.md](../plans/0010-headless-rendering-foundation.md) (`Approved`, Human Review Approval recorded 2026-08-16 following two independent Plan Review rounds — see the plan's own Human Review Approval note, [PR #45](https://github.com/slmao/Atlantis/pull/45), [PR #46](https://github.com/slmao/Atlantis/pull/46) — including corrections for a missed private-access call site, an exception-throwing `dynamic_cast` form, and a stale dependency claim) | **Implemented and merged** via [PR #48](https://github.com/slmao/Atlantis/pull/48). Implements this Spec's design in full, strictly against Plan 0010's Step 1–6 sequencing: a new `atlantis::rhi::OffscreenTarget` interface, `ResourceState::TransferSource`/`BufferPurpose::Readback`/`CommandList::copyRenderTargetToBuffer()` for GPU-to-CPU readback, `ResourceBinding`'s `incomingState`/`finalState` generalization of `render_graph::execute()`, `Renderer::drawFrame()`'s new required `finalColorState` parameter, and the Vulkan Backend's `VulkanOffscreenTarget`/`VulkanOffscreenRenderTarget` plus a shared private `VulkanRenderTargetAccess` boundary adopted via checked pointer-form `dynamic_cast` at all five affected call sites (never a thrown exception). New `examples/headless_rendering_demo` and `tests/vulkan_backend/headless_rendering_gpu_tests.cpp`. **Verified** (no CI pipeline exists yet; verification below was run locally, per PR #48's own final "Testing performed" and "Manual verification record"): Debug and Release builds clean; GPU-independent tests 244/244 Debug, 243/243 Release; GPU-required tests 12/12 both configs on real hardware (Intel Arc B370, integrated GPU, Vulkan 1.4.335) — 4 pre-existing windowed regression cases plus 8 new headless cases, Vulkan Validation Layers clean throughout; `examples/headless_rendering_demo` runs to completion with its own basic content check passing on all 3 cycles, both configs; **interactive windowed regression** (`frame_execution_demo`/`minimal_renderer_demo`, Debug and Release — window/visible frame, continuous resize, minimize/restore, mid-frame exit, clean exit) performed and observed directly by a human verifier, all 4 configuration/demo combinations PASS, Validation Layers clean. Four files outside the Plan's own enumerated list needed the same mechanical `finalState`/`drawFrame()` fix the Plan's own three sites already required (disclosed in PR #48's own "Deviations from Plan" section) — no design implication. **Disclosed limitation:** all real-hardware verification above ran on a single GPU vendor/driver (Intel Arc, integrated) — no second GPU vendor or driver combination was available in this environment; this is not cross-vendor coverage. Golden-image comparison, tolerance methodology, and CI image-regression gating remain explicitly out of this Spec's scope — see its own Non-Goals; Image Regression Testing now has its own Spec — see Spec 0011 below. | Depends on Spec 0007 (Minimal Renderer) — `Approved`, implemented. [ADR-0038](../adr/0038-headless-offscreen-rendertarget-construction-and-ownership.md)–[ADR-0040](../adr/0040-gpu-to-cpu-readback-rhi-capability.md), all `Accepted`, plus an **Accepted Amendment** to [ADR-0022](../adr/0022-minimal-renderer-public-api-and-resource-ownership.md) (`Renderer::drawFrame()`'s new `finalColorState` parameter) — all accepted alongside this spec's Human Review Approval recorded 2026-08-16. Promoted from Candidate Backlog Section B below (formerly "Headless Rendering," Candidate Order 2). |
| 0011 | [Image Regression Testing Foundation](0011-image-regression-testing-foundation.md) | `Approved` | [plans/0011-image-regression-testing-foundation.md](../plans/0011-image-regression-testing-foundation.md) (`Approved`, Human Review Approval recorded 2026-08-17 following one independent Plan Review round and one independent Human Review — see the plan's own Human Review Approval note, [PR #51](https://github.com/slmao/Atlantis/pull/51) — including corrections for a `vulkan_requested_instance_api_version` data-accuracy bug, an unspecified actual/diff failure-artifact-writing step, and resolution of all four originally-flagged open design questions) | **Implemented and merged** via [PR #52](https://github.com/slmao/Atlantis/pull/52), strictly against Plan 0011's Step 1–7 sequencing — see the PR's own commit list for the exact, current commit-by-commit breakdown (harness; the first committed golden, added via its own separate commit per ADR-0042's ordering rule; several small, disclosed mechanical fixes found only by actually running the code, not by inspection alone). `tests/image_regression/` now exists: `support/` (`Atlantis::ImageRegressionSupport` — pixel-diff comparison, PNG codec, provenance sidecar parsing, the four-step golden validity check), `fixture/` (`minimal_cube_fixture`, duplicating `examples/headless_rendering_demo`'s own setup/render sequence byte-for-byte), `golden_generator/` (a standalone, non-CTest-registered tool requiring a clean `git` working tree), and `goldens/minimal_cube/` (the first committed golden PNG + sidecar, captured on the reference machine — Intel Arc B370, driver 101.8509, Vulkan 1.4.357/1.4.335). Golden-update-reason category (ADR-0042): **Initial baseline bootstrap** — the fourth category ADR-0042's own Accepted Amendment (2026-08-17) adds specifically for a scene's first-ever golden, adopted after an earlier revision of this row first miscategorized this update as an unreviewed, non-existent "harness bootstrap" label, then as a stopgap "Approved rebaseline" (the closest existing fit at the time, but a mismatch with that category's own old-vs-new-diff evidentiary bar, which cannot apply when no prior golden exists). All six of the new category's own constraints are satisfied: the golden did not exist on `main` before this PR and per the amendment's own rule may never use this category again once committed; source revision, full provenance, and separate-commit ordering all follow the unrelaxed general rule; the inapplicable old-vs-new diff is substituted with direct visual review (confirmed a correctly-rendered, non-black, centered cube), the build-time comparator's own zero-difference self-consistency check, a real GPU/Validation-Layers run, and this ADR's own already-recorded empirical calibration evidence. `stb` (`nothings/stb`) added as a new test-only `FetchContent` dependency per ADR-0041, linked only into these targets. **Verified for real, not merely compiled:** Debug/Release builds clean; `ctest -LE gpu` 274/274 Debug, 273/273 Release; `ctest -L gpu` 16/16 both configs against the real committed golden with **zero** channel difference across all 1,048,576 bytes; Vulkan Validation Layers clean throughout (grepped, not merely absence-of-failure); the golden generator's clean-tree rejection path exercised against a genuinely dirty tree; a **deliberate rendering regression** (one fixture vertex's Y position temporarily changed, rebuilt, re-run, then reverted before this PR opened) was genuinely caught (`outOfToleranceCount = 9,873`, `3.77%`) with `_actual.png`/`_diff.png` written and visually confirmed to highlight exactly the perturbed region. No RHI, RenderGraph, Renderer, or Vulkan Backend file touched. | Depends on Spec 0010 (Headless Rendering Foundation) — `Approved`, implemented via PR #48, dependency satisfied. [ADR-0041](../adr/0041-image-regression-testing-golden-image-data-format-and-codec-dependency.md) (golden image data format and codec dependency) and [ADR-0042](../adr/0042-image-regression-testing-comparison-methodology-and-test-ownership-boundary.md) (comparison methodology and test ownership boundary), both `Accepted` alongside this spec's Human Review Approval recorded 2026-08-16; ADR-0042 additionally carries an **Accepted Amendment** (2026-08-17, adding the "Initial baseline bootstrap" golden-update-reason category) recorded via [PR #53](https://github.com/slmao/Atlantis/pull/53). Promoted from Candidate Backlog Section B below (formerly "Image Regression Testing," Candidate Order 1). |
| 0012 | [Asset System Foundation](0012-asset-system-foundation.md) | `Approved` | [plans/0012-asset-system-foundation.md](../plans/0012-asset-system-foundation.md) (`Approved / Ready for Implementation`) | **Implemented and merged** via [PR #58](https://github.com/slmao/Atlantis/pull/58) — strictly against Plan 0012's Step 1–7 sequencing (Steps 1–4a/5/6 incremental, Step 4b atomic per the Plan's own atomic-step boundary), followed by an independent implementation review round (commit `bc7fc02`, merged in the same PR) that found and fixed real gaps before merge: Verification V11 (atomic writes) had been specified but never actually tested — added genuine overwrite-of-an-existing-destination, failed-write-leaves-no-temp-file, failed-cook-after-prior-success-preserves-existing-output, and a forced real `rename()`-failure test; `mesh_source.cpp`'s `index_count`-driven allocation was unbounded before validation, now bound-checked; `load.cpp` gained a metadata self-consistency check (its own `asset_id` against its own `source_logical_path`, reusing the existing `MetadataArtifactMismatch` error — no new enumerator); `main.cpp` gained a top-level exception safety net. `src/asset_system/` (`Atlantis::AssetSystem`, Core-only), `src/tools/asset_cooker/` (Tools-hosted CLI), `assets/meshes/minimal_cube.mesh.txt` (the one real authoring asset, transcribed from `tests/image_regression/fixture/minimal_cube_fixture.cpp`'s own vertex/index arrays), `tests/asset_system/` (including `rename_overwrite_probe.cpp`, added during the review round to empirically verify Windows rename-replace semantics — one file beyond the Plan's own original list, disclosed in its own commit), and `tests/tools/asset_cooker/` all exist. One disclosed mechanical deviation: the D9 path-normalization algorithm's drive-letter check runs before, not after, the general allowed-character sweep — as originally ordered, `DriveLetterRejected` could never be reached, since `:` was never itself an allowed byte; this is a bug-fix in step ordering, not a change to any accepted decision. **Verified for real, final state after the review round:** independent, fresh Debug and Release builds from a clean tree (including an explicit parallel build); `ctest -LE gpu` 371/371 (Debug) and 370/370 (Release, one fewer due to the pre-existing Debug-only `ATLANTIS_ASSERT` test, unrelated to this Spec); `ctest -L gpu` 17/17 both configurations, including the asset-sourced case achieving **zero** channel difference against the existing, unmodified Spec 0011 golden — the golden itself is untouched by this implementation; the full Plan 0012 Section D7 re-import-triggering procedure run twice (once per round) and confirmed exactly as specified both times; Vulkan Validation Layers grepped clean both configurations (zero VUID/Validation Error/Warning matches); a module-boundary include scan confirming `src/asset_system/` depends on Core only. | Depends on Spec 0007 (Minimal Renderer) — `Approved`, implemented, dependency satisfied; explicitly does not wait for Atlantis Runtime (now Spec 0013, `Approved`, Section A above — not yet specced at the time of this row's own drafting) — see the spec's own "Why this does not wait for Runtime." [ADR-0043](../adr/0043-asset-system-module-boundary.md) (module boundary), [ADR-0044](../adr/0044-asset-system-identity-provenance-and-import-methodology.md) (identity, provenance, and import methodology), and [ADR-0045](../adr/0045-asset-system-data-format-versioning-and-dependency-policy.md) (data format, versioning, and dependency policy) — split from an originally combined ADR-0043 draft 2026-08-18 at Human Review's own request — all three `Accepted` alongside this spec's Human Review Approval recorded 2026-08-19, which additionally directed three corrections applied before approval: Asset System depends on Atlantis Core alone and hands CPU-side `StaticMeshAssetData` to a composition root rather than creating GPU meshes itself; the runtime artifact's on-disk byte order is an unconditional little-endian format contract; and collision detection is scoped to one importer/validator invocation's declared asset set, with repository-global uniqueness delegated to a Plan-arranged validation step and any future asset registry. Also builds on [Spec 0009](0009-long-term-engine-architecture-alignment.md) (`Approved`) and its ADR-0032–ADR-0035 (all `Accepted`), which this spec's own Architectural Impact explicitly satisfies. Promoted from Candidate Backlog Section B below (formerly "Asset System Foundation," Candidate Order 2) — drafted ahead of Android Platform (Candidate Order 1) by explicit human direction; see Section B's own updated ordering note. |
| 0013 | [Runtime Host Foundation](0013-runtime-host-foundation.md) | `Approved` | [plans/0013-runtime-host-foundation.md](../plans/0013-runtime-host-foundation.md) (`Approved / Ready for Implementation`, joint Spec + Plan Human Review recorded 2026-08-21 — see that Plan's own Human Review Approval note) | **Implemented and merged** via [PR #63](https://github.com/slmao/Atlantis/pull/63), strictly against Plan 0013's Step 1–7 sequencing, following [PR #61](https://github.com/slmao/Atlantis/pull/61) (Spec + Plan) and [PR #62](https://github.com/slmao/Atlantis/pull/62) (a Plan amendment correcting D3/V3's enum-exhaustiveness mechanism before implementation continued — MSVC's `C4715` does not, by itself, detect an incomplete `switch`; real protection is `/w14062`'s `C4062` diagnostic, target-scoped to `atlantis_runtime_host` only). `src/runtime/` (`atlantis_runtime_host` static library, alias `Atlantis::RuntimeHost`, plus the thin `atlantis_runtime` Windows executable) and `tests/runtime/` now exist and match the spec/plan: a `PlatformSession` RAII guard declared as `RuntimeApplication`'s first member (destroyed last, by ordinary C++ reverse-declaration-order destruction) makes window-outlives-GPU-resources a compiler-enforced invariant; a six-step initialization, a ten-step per-frame orchestration, and a single idempotent `shutdown()` compose Platform, RHI, Vulkan Backend, Renderer, Shader System, and Asset System into one real Windows windowed composition root, drawing the same Asset-System-sourced `minimal_cube` mesh and Shader-System-compiled material used by earlier milestones. A post-implementation independent review round (commit `131b49a`, merged in the same PR) found and fixed a real lifetime hazard before this row was written "merged": both `PlatformSession` and `RuntimeApplication` originally kept a move-assignment operator that could call `platform::shutdown()` a second time, or tear the window down ahead of still-live GPU resources; both operators are now deleted, move-construction-only, locked by `tests/runtime/runtime_ownership_tests.cpp`'s own `static_assert`s. **Verified for real, final state after the review round:** independent, fresh Debug and Release builds; `ctest -LE gpu` 389/389 (Debug) and 388/388 (Release); `ctest -L gpu` 18/18 both configurations, including `runtime_smoke_gpu_tests` (the first `gpu`-labeled test in the repository to open a real, visible OS window during automated `ctest` — three real acquire/draw/submit/present cycles); Vulkan Validation Layers grepped clean both configurations (zero `VUID`/`Validation Error`/`Validation Warning` matches); `platform::shutdown()` confirmed to have exactly one real call site in `src/runtime/` (`PlatformSession::~PlatformSession()`) by grep, not inspection alone; `C4062` positive/negative re-verified in-project (a temporarily removed enumerator case fails the build naming that exact enumerator; restoring it builds clean again). Interactive resize/minimize/restore/close were verified programmatically (real Win32 `SetWindowPos`/`WM_SYSCOMMAND`/`WM_CLOSE` message injection plus process-liveness/exit-code checks) against the real `atlantis_runtime.exe`, confirming lifecycle correctness (clean start, clean `RuntimeExitReason::Success` exit, no crash, no assertion failure). **Disclosed limitation, unresolved as of this row:** no automated literal pixel/visual screenshot comparison of the Runtime window's rendered output exists for Spec 0013 — standard Win32 screen-capture APIs were confirmed unusable from the implementing agent session (captured the session's own UI, not the real desktop, reproduced identically against an unmodified prior demo). The programmatic interactive verification above is lifecycle/liveness evidence, not a pixel comparison; the pre-existing `image_regression_gpu_tests` golden match (Spec 0011/0012) is pixel-exact evidence for the same mesh/shader/camera combination in its own headless fixture, not for `atlantis_runtime`'s own windowed output — neither substitutes for an actual by-eye windowed check of `atlantis_runtime.exe` against the existing golden, which PR #63 recorded as an outstanding, human-only verification step at merge time. | Depends on Spec 0002 (Platform Foundation), Spec 0003 (RHI and Vulkan Windowed Foundation), Spec 0005 (RenderGraph Foundation), Spec 0007 (Minimal Renderer), Spec 0008 (Shader System Foundation), and Spec 0012 (Asset System Foundation) — all `Approved`, implemented; this spec's own analysis found Spec 0008 and Spec 0012 necessarily load-bearing (a real Runtime cannot draw anything without a compiled shader, and this spec's own bootstrap scene is Asset-System-sourced), correcting the Candidate Backlog's own original, narrower dependency list. [ADR-0046](../adr/0046-runtime-composition-ownership-and-frame-lifecycle.md) (Runtime composition, object ownership, and frame lifecycle) and [ADR-0047](../adr/0047-runtime-host-executable-library-structure-and-test-boundary.md) (Runtime Host executable/library structure and test boundary), both `Accepted` alongside this spec's own Human Review Approval, recorded 2026-08-20 (see the spec's own Human Review Approval note — all twelve accepted-scope items listed there). An independent review round (found and fixed one substantive and three mechanical drafting errors, including a corrected windowed-vs-headless verification model) preceded this approval, disclosed in full in the spec's own "Independent Review — Round 2" note. Promoted from Candidate Backlog Section B below (formerly "Runtime Host and Composition Root," Candidate Order 2) — drafted ahead of Android Platform (Candidate Order 1) by explicit human direction, matching the same pattern Spec 0012 already used; see Section B's own updated ordering note. |
| 0014 | [World / Scene Foundation](0014-world-scene-foundation.md) | `Approved` | [plans/0014-world-scene-foundation.md](../plans/0014-world-scene-foundation.md) (**`Approved / Ready for Implementation`**, Human Review Approval recorded 2026-08-22, following four independent Plan Review rounds. Round 2 found a cross-`World`-instance `EntityId` gap in the `Accepted` contract and blocked approval on it rather than resolving it silently, proposing two options without deciding between them. Round 3 applied Human Review's own directed design — a stable, heap-allocated, address-stable per-`World` identity token (not a global counter), `EntityId` growing a third field, a new `WorldError::WrongWorld`. Round 4 applied two further Human-Review-added refinements (the identity field made `private`, not a writable public raw pointer; an explicit moved-from-`World` contract) and recorded formal Human Review Amendment Approval on both governing amendments below. See the Plan's own Human Review Approval note for its full scope.) | **Implementation merged via [PR #68](https://github.com/slmao/Atlantis/pull/68).** [PR #67](https://github.com/slmao/Atlantis/pull/67) (Plan 0014) merged 2026-08-22, satisfying the merge gate; Implementation then followed Plan 0014's Step 1–10 sequencing strictly, one commit per step, Step 8's golden capture in its own separate commit per ADR-0042. PR #68 disclosed that `getRenderable()` reused `WorldError::NoCameraComponent` for a missing `Renderable`; Human Review **rejected** that reuse and directed a fifth, independent enumerator — recorded via a governance-only "Human Review Correction (2026-08-23)" merged in [PR #69](https://github.com/slmao/Atlantis/pull/69) (ADR-0049, Spec 0014, Plan 0014), with the corresponding `WorldError::NoRenderableComponent` code fix, new V28, and a broadened final code review, all merged via [PR #70](https://github.com/slmao/Atlantis/pull/70). Delivers `src/world/` (`Atlantis::World`, an 11th top-level module — entity lifecycle, hierarchy, math contract/`updateTransforms()`, Camera/Renderable/active-camera), a Runtime-private extraction adapter (`src/runtime/scene_extraction.h`/`.cpp`), a World-driven six-entity validation scene wired into `RuntimeApplication`, a new headless `world_scene` image-regression golden (existing `minimal_cube` golden untouched), and the windowed GPU smoke test extended to confirm 5 `DrawItem`s reach `drawFrame()`. Verified for real: clean Debug and Release builds; `ctest -LE gpu` 436/436 Debug, 435/435 Release (V28's own two new test cases included); `ctest -L gpu` 19/19 both configurations, all on real Vulkan-capable hardware (Intel Arc B370, driver 101.8509), Vulkan Validation Layers grepped clean throughout (zero VUID/Validation Error/Warning matches). Programmatic interactive automation (real Win32 message injection for resize, minimize/restore, close) against the real `atlantis_runtime` executable was performed as lifecycle/liveness evidence only, never recorded as V20's own genuine-human, GUI-based visual confirmation. **V20 was subsequently satisfied 2026-08-23**: an actual human personally observed both Debug and Release `atlantis_runtime` (five-cube scene matching the golden, interactive resize/minimize/restore/close, clean exit code 0, Validation Layers zero VUID/Error/Warning in both runs), recorded as a PR comment on [PR #70](https://github.com/slmao/Atlantis/pull/70). Two disclosed, mechanical (non-architectural) deviations from the Plan's own illustrative pseudocode: `Result<std::monostate, WorldError>` used throughout in place of the Plan's own `Result<void, WorldError>` (this codebase's actual `Result<T,E>` cannot hold `void`; `Result<std::monostate, E>` is the same convention every other already-implemented module, and this same Plan's own `scene_extraction.h`, already uses); `tests/runtime/init_error_tests.cpp` created fresh rather than "extended" (the Plan's own file list assumed it already existed; it did not). See Plan 0014's own PR for the full commit-by-commit breakdown and disclosed-deviations record. | Depends on Spec 0013 (Runtime Host Foundation) — `Approved`, implemented, dependency satisfied. [ADR-0048](../adr/0048-world-scene-module-boundary-and-ownership.md)–[ADR-0051](../adr/0051-world-to-renderer-extraction-and-asset-resolution-boundary.md), all `Accepted`, alongside this spec's own Human Review Approval recorded 2026-08-22 (see the spec's own Human Review Approval note — all 17 accepted-scope items listed there, mapping to the merged document's own 14-row Human Review Decision Table plus the deterministic slot-reuse/enumeration contract, the full Math contract, and the Spec's own enumerated Non-Goals). **Both this Spec and ADR-0049 additionally carry an "Accepted Amendment" section (2026-08-22, formal Human Review Amendment Approval recorded)** — the stable, `private`-field `World` identity token design, found necessary during Plan 0014's own Round 2 review, directed by Human Review in Round 3, and finalized with the private-encapsulation and moved-from-`World` refinements in Round 4; see each document's own section and Plan 0014's Human Review Approval / Deviations. Promoted from Candidate Backlog Section B below (formerly "World/ECS Foundation," Candidate Order 2) — drafted immediately following Spec 0013's post-merge closeout ([PR #64](https://github.com/slmao/Atlantis/pull/64)), by explicit human direction to continue World/Scene work next; see Section B's own updated ordering note. |
| 0015 | [Scene Asset & Serialization Foundation](0015-scene-asset-serialization-foundation.md) | `Approved` | [plans/0015-scene-asset-serialization-foundation.md](../plans/0015-scene-asset-serialization-foundation.md) (**`Approved / Ready for Implementation`**, Human Review Approval recorded 2026-08-23, following two Independent Review rounds — the second of which found and fixed a real compile error (`RuntimeApplication::world_` retyped to `std::optional<World>`, since `World` is not move-assignable), a misleading load-order description, an over-triggering CMake dependency, and completed a switch-exhaustiveness inventory; see the Plan's own Human Review Approval note for its full scope) — drafted and revised against real, current `src/asset_system/`, `src/world/`, `src/runtime/`, and CMake asset-declaration source; no architectural blocker found. [PR #73](https://github.com/slmao/Atlantis/pull/73) merged 2026-08-23, satisfying Implementation's own gate. | **Implementation merged via [PR #74](https://github.com/slmao/Atlantis/pull/74)** (2026-08-23), following Plan 0015's own 10 Milestone steps, one commit per step. A subsequent, pre-merge centralized final review (still on PR #74, no new PR) removed a test-only friend (`ValidatedSceneDataTestAccess`) that had weakened `ValidatedSceneData`'s own "`decodeScene()` is the sole privileged entry point" contract — confirmed by full-repo grep that the private constructor now has exactly one call site anywhere in this codebase — and added `static_assert`-based, compile-time proof (not prose) that `RuntimeApplication`'s own scene-load publish (`world_.emplace()` then `meshResourceMap_`'s own move-assignment) is genuinely atomic, both operations being unconditionally `noexcept`. Verified: Debug `ctest -LE gpu` 516/516, Release 515/515; `ctest -L gpu` 20/20 both configurations; the new `world_scene_loaded` GPU test matches the existing, unmodified `world_scene` golden with zero pixel difference; Vulkan Validation Layers zero warnings/errors throughout. **V24 (genuine human visual confirmation) is PASS** — a human launched real, visible Debug and Release windows separately, confirmed the five-cube composition against the golden, resize, minimize/restore, and a clean close for both, with literal process exit code 0 for both; recorded on PR #74's own comments, not inferred from the automated GPU tests. See the PR's own body/comments for the full, disclosed list of implementation-time deviations (none changing any Approved file format, public API, module boundary, error model, or transactional contract). | Depends on Spec 0012 (Asset System Foundation) and Spec 0014 (World / Scene Foundation) — both `Approved`, implemented, dependencies satisfied. [ADR-0052](../adr/0052-scene-asset-module-boundary-and-ownership.md)–[ADR-0054](../adr/0054-scene-loading-transactional-instantiation-contract.md), all `Accepted`, alongside this spec's own Human Review Approval recorded 2026-08-23 (see the spec's own Human Review Approval note — all 16 items of its own Human Review Decision Table accepted, as recommended, with no amendment, including the `ValidatedSceneData` unforgeability contract, the per-scene build-tree-private dependency manifest, and the explicit disclosure that a distributable Asset Catalog remains future work, not solved here). **ADR-0053 additionally carries a "Human Review Correction" section (2026-08-23, formal Human Review Approval recorded)** — `ValidatedSceneData` has no public default constructor of any kind (a zero-node scene is now an explicit, named cook-time/decode-time error, `EmptyScene`), resolving a wording gap between this ADR's own original Decision text and Spec 0015's own Human Review Approval summary; see that ADR's own Correction and the Plan's own Human Review Approval item 3. Promoted from Candidate Backlog Section B below (formerly "Serialization and Stable Identity," Candidate Order 2) — the human maintainer directly selected and narrowed this candidate's own scope to a scene-asset bake/load loop (see Section B's own updated note for the full disclosed narrowing); drafted immediately following Spec 0014's own post-merge closeout ([PR #71](https://github.com/slmao/Atlantis/pull/71)). |
| 0016 | [Texture & Sampler Foundation](0016-texture-sampler-foundation.md) | `Approved` | [plans/0016-texture-sampler-foundation.md](../plans/0016-texture-sampler-foundation.md) (`In Review`) — drafted following this Spec's own Human Review Approval (2026-08-24); a centralized self-review pass (2026-08-24) found and fixed a real GPU-required-step ordering error, resolved the `cookTexture()`/`stb`-boundary module-placement question left open by the Spec, decoupled `TextureAssetData`'s own color-space field from `atlantis::rhi::SampledTextureFormat` to preserve Asset System's Core-only dependency, and corrected `Material`'s new fields from non-nullable references to nullable, both-or-neither pointers — see that Plan's own Independent Review note | **Not started — Human Review Approval recorded 2026-08-24 (see the Spec's own Human Review Approval note — all 18 items of its own Human Review Decision Table accepted, as recommended, with no amendment); no Implementation is authorized by this approval alone.** Pre-draft verification against real, current source confirmed today's `Texture` is depth-only (`DepthFormat` a single-variant enum), RHI has no `Sampler` concept, no CPU→GPU copy command exists (only the Spec 0010 GPU→CPU readback direction), RenderGraph's `ResourceBinding` has no generic sampled-resource binding kind, `Material` has no descriptor-binding API beyond one hardcoded uniform buffer, Shader System reflects only that one uniform-buffer binding kind, and `StaticMeshAssetData`/the mesh artifact carry no UV attribute at any layer. Per AGENTS.md's own Golden Rule ("Render Graph is the mandatory path for GPU work"), the one-time texture upload cannot be a raw `CommandList` sequence outside RenderGraph — RenderGraph itself gains a real, new resource-binding kind, not merely RHI/Vulkan Backend. **A centralized, evidence-driven final review round (2026-08-23, still on this Spec's own first PR) found and corrected two further real gaps before Human Review**: (1) `Device::submit()` has exactly one overload and unconditionally requires a real, module-produced `RenderTarget` — there is no target-free submit path; the corrected upload design reuses the fixture's own already-acquired `OffscreenTarget`-vended `RenderTarget` for the upload's own `submit()` call, with the existing `Device::waitIdle()` as the real completion mechanism, introducing no new `Device`/`Renderer` public API. (2) "Hand-authored UV, zero shared-type changes" (the first draft's own recommendation for the UV question) is not actually achievable — `VertexAttributeFormat` (RHI) has no `Float2` variant anywhere in the pipeline, blocking a real UV attribute for *any* fixture; the corrected recommendation adds `VertexAttributeFormat::Float2` (RHI) and `VertexAttributeType::Float2` (Shader System) — a small, disclosed, mechanical addition fully decoupled from Asset System's own mesh pipeline (`StaticMeshAssetData`/the mesh authoring grammar/artifact remain untouched) — so the new fixture's own UV0 is a real, `Mesh`-bound vertex attribute, not a fabricated or position-derived one, while still not claiming a real asset-sourced textured mesh exists. A third, previously-dead-code gap was also found and fixed: the shader-compiler tool's own `expectedContract` field was parsed from CMake but never consulted, which would have failed this Spec's own new shader's build-time contract validation regardless of the UV decision. **A second, targeted revision round (2026-08-24, same PR) corrected two further real gaps**: (1) the upload was first designed as its own, separate `submit()`/`waitIdle()` cycle before a later, separate per-frame draw graph — corrected so the upload, the real draw, and a readback all share **exactly one** `Device::submit()` call against a `RenderTarget` that genuinely participates (never a dummy token), with `ShaderRead` correctness now stated as coming from the upload's own barrier and recorded execution order *within* that one submission, and `Device::waitIdle()`'s own role narrowed to its real, purely CPU-side purpose (safe to read back, safe to destroy staging/readback buffers) — matching `headless_rendering_gpu_tests.cpp`'s own closest existing precedent more closely than the withdrawn two-submission design. (2) `Rgba8Srgb`'s own hardware linearization-on-sample is real GPU behavior a CPU-only artifact round-trip test cannot prove; the fixture now cooks the same source image twice (`Rgba8Unorm` and `Rgba8Srgb`) and samples both as two quads in the *same* golden, proving both real GPU sampling paths rather than asserting one supported while testing only the other. Target-independent submission (an upload with no real `RenderTarget` at all) is explicitly registered as future work, not solved with a workaround. "Mesh UV Attribute Foundation" (real UV0 inside Asset System's own mesh pipeline) remains a named, immediate, blocking follow-up candidate for a future asset-sourced-textured-mesh claim. | Depends on Spec 0003 (RHI/Vulkan Backend construction boundary) — `Approved`, implemented, dependency satisfied. [ADR-0055](../adr/0055-sampled-texture-and-sampler-rhi-module-boundary-and-ownership.md)–[ADR-0057](../adr/0057-texture-asset-format-decoder-dependency-and-color-space-contract.md), all `Accepted`, alongside this Spec's own Human Review Approval recorded 2026-08-24. ADR-0057's own decision 4 additionally required a companion amendment to the already-`Accepted` [ADR-0041](../adr/0041-image-regression-testing-golden-image-data-format-and-codec-dependency.md) (widening `stb_image`'s linkage from test-only to Tools/cooker use) — **ADR-0041's own "Accepted Amendment — 2026-08-24" section** (its own original `Accepted` Decision/Consequences/Alternatives Considered left completely unmodified above it) was accepted in the same Human Review pass; it records both the boundary widening and a real CMake configure-ordering defect this review round found (`cmake/AtlantisDependencies.cmake`'s `stb` declaration is included only inside `if(ATLANTIS_BUILD_TESTS)`, after `add_subdirectory(src/tools/asset_cooker)` already runs) and its own fix. **Known follow-up items remain explicitly tracked, not resolved by this approval**: "Mesh UV Attribute Foundation" (real UV0 inside Asset System's own mesh pipeline, required before any future asset-sourced-textured-mesh claim) and target-independent submission (a `Device::submit()` path admitting no real `RenderTarget`) are both named, disclosed future decisions this Spec explicitly does not solve. Promoted from Candidate Backlog Section B below (formerly "Texture & Sampler Foundation," Candidate Order 8) — human-selected 2026-08-24 to be drafted next, ahead of Android Platform (Candidate Order 1, whose own priority remains unchanged by this approval); drafted immediately following Spec 0015's own post-merge documentation closeout. |

No spec above is marked `Complete` — this registry only uses the status
values `specs/README.md` and `plans/README.md` already define
(`Draft` / `In Review` / `Approved` / `Rejected` / `Superseded`, and for
plans additionally `Approved / Ready for Implementation`). Where a
spec's Plan records an explicit Human Review approval note (Specs 0001,
0002, and 0003 do, with dates, in their own Plan files), that is the
authoritative record — this table does not restate it, only points to
it. Plan 0003 records a joint Spec + Plan Human Review completed
2026-08-08; Spec 0003 itself remains `Approved` (an approved spec does
not change status when its plan is implemented). This table's
Implementation column records the fact that PR #14 merged Spec 0003's
implementation into `main`; the registry itself grants no new approval
and is not a substitute for reading the Plan or PR.

### B. Candidate Spec Backlog

**Not an approved commitment.** This is a dependency-ordered list of
candidate future work, for planning visibility only. No entry here has
a Spec written, and none is authorized for implementation — see
[AGENTS.md](../AGENTS.md) and
[docs/project-blueprint.md](../docs/project-blueprint.md) Section 1.
No formal spec number is pre-assigned to any entry; numbers are
assigned only when a real spec is drafted.

**Confirmed working priority order (human-directed 2026-08-15, confirmed
2026-08-16 alongside Spec 0010's Human Review Approval):** Headless
Rendering Foundation (Spec 0010, `Approved` — Section A above,
**implemented and merged via PR #48**) → Image Regression Testing
(**Spec 0011, `Approved` — Section A above, Human Review Approval
recorded 2026-08-16 following two independent review rounds**) →
Android Platform and Vulkan Presentation. Nothing about Android
Platform's own dependencies or scope changes — it still depends only on
Spec 0002/Spec 0003, both implemented, and remains independent of
Headless Rendering/Image Regression Testing in either direction. This
part of the record is historical and unchanged: it correctly predicted
and confirmed the Headless Rendering → Image Regression Testing
sequence, both now complete.

**Superseded in part, human-directed 2026-08-18:** with Headless
Rendering and Image Regression Testing both complete, the human
maintainer directed Asset System Foundation — not Android Platform — to
be drafted next (now Spec 0012, `Approved`, **implemented**, Section A
above; see that spec's own "Why this does not wait for Runtime" for why
this ordering does not conflict with Asset System's own dependencies).
This is an explicit human reprioritization, not an agent-made scheduling
decision and not a finding that Android Platform's own priority, scope,
or dependencies have changed. **Android Platform and Vulkan Presentation
remains `Candidate`, at its own unchanged Candidate Order 1 position
below, still depending only on Spec 0002/Spec 0003 (both
implemented).**

**Superseded in part again, human-directed 2026-08-20:** with Asset
System Foundation now `Approved`/implemented, the human maintainer
directed Runtime Host and Composition Root — not Android Platform — to
be drafted next (now Spec 0013, `Approved`, Section A above). This is
again an explicit human reprioritization, not an agent-made scheduling
decision and not a finding that Android Platform's own priority, scope,
or dependencies have changed. **Android Platform and Vulkan Presentation
remains `Candidate`, at its own unchanged Candidate Order 1 position
below, still depending only on Spec 0002/Spec 0003 (both
implemented).**

**Superseded in part again, human-directed 2026-08-22:** with Runtime
Host Foundation now `Approved`/implemented (Spec 0013, Section A above),
the human maintainer directed World/ECS Foundation — not Android
Platform — to be drafted next (now Spec 0014, `Approved`, Section A
above; title "World / Scene Foundation"). This is again an explicit
human reprioritization, not an agent-made scheduling decision and not a
finding that Android Platform's own priority, scope, or dependencies
have changed. **Android Platform and Vulkan Presentation remains
`Candidate`, at its own unchanged Candidate Order 1 position below,
still depending only on Spec 0002/Spec 0003 (both implemented).**

**Superseded in part again, human-directed 2026-08-23:** with World /
Scene Foundation now `Approved`/implemented (Spec 0014, Section A
above), the human maintainer directly selected "Serialization and
Stable Identity" — not Android Platform — to be drafted next (now Spec
0015, `Approved`, Section A above; title "Scene Asset & Serialization
Foundation"). This is again an explicit human reprioritization, not an
agent-made scheduling decision and not a finding that Android
Platform's own priority, scope, or dependencies have changed.
**Android Platform and Vulkan Presentation remains `Candidate`, at its
own unchanged Candidate Order 1 position below, still depending only on
Spec 0002/Spec 0003 (both implemented).** **Disclosed scope narrowing,
not silent drift:** this candidate's own prior description here read
"Stable GUID/handle schemes, schema versioning and migration"; the
human maintainer's own direction, and Spec 0015's own drafting, narrow
this to a scene-asset authoring/cook/load pipeline using scene-local,
per-artifact node identity — explicitly **not** the broader durable,
cross-session, cross-artifact stable-identity/migration system the
original description implied. That broader scope remains future,
unscoped work — see Spec 0015's own Non-Goals and Out of Scope / Future
Work sections for the full disclosure.

**Superseded in part again, human-directed 2026-08-24:** with Scene
Asset & Serialization Foundation now `Approved`/implemented (Spec
0015, Section A above), the human maintainer directly selected
"Texture & Sampler Foundation" — not Android Platform — to be drafted
next. This is again an explicit human reprioritization, not an
agent-made scheduling decision and not a finding that Android
Platform's own priority, scope, or dependencies have changed.
**Android Platform and Vulkan Presentation remains `Candidate`, at its
own unchanged Candidate Order 1 position below, still depending only
on Spec 0002/Spec 0003 (both implemented).** Texture & Sampler
Foundation itself has since been drafted as [Spec 0016](0016-texture-sampler-foundation.md)
(`In Review`, Section A above) and no longer appears in the table
below — see that spec's own row for its current status. This note is a
scheduling-decision record only, not a spec approval or an
authorization to implement.

**This was, and remains, a confirmed ordering for which candidate gets
specced next, not an approval of any candidate's own scope or an
authorization to implement any of them.** Status changes for a
candidate itself come only from a real Spec, Plan, or merged PR, never
from this ordering note (see Backlog maintenance rules below). This
note does not itself authorize Implementation for Spec 0011, Spec
0012, Spec 0013, or Spec 0014 — those approvals come from each spec's
own Human Review Approval, recorded on its own document, not from this
ordering note.

| Candidate Order | Working Title | Depends On | Intended Outcome | Governance State |
|---|---|---|---|---|
| 1 | Android Platform and Vulkan Presentation | Spec 0002 (Platform interface), Spec 0003 (RHI/Vulkan Backend construction boundary) | Android Activity/Surface lifecycle, `ANativeWindow` borrowed semantics, `VK_KHR_android_surface` WSI, reusing the existing RHI/RenderGraph/Renderer stack unchanged | Candidate — spec not yet drafted |
| 2 | Tool/Editor Connection Protocol | Spec 0013 (Runtime Host Foundation) | Editor↔Runtime communication boundary — in-process vs. multi-process model not chosen | Candidate — spec not yet drafted |
| 3 | Gameplay SDK | Spec 0014 (World / Scene Foundation), Candidate 2 (Tool/Editor Connection Protocol) | First-party gameplay authoring surface — gameplay language not chosen | Candidate — spec not yet drafted |
| 4 | Research/Simulation API | Spec 0013 (Runtime Host Foundation), Spec 0010 (Headless Rendering Foundation) | Observation/Action/Episode-style research interface — language/transport not chosen | Candidate — spec not yet drafted |
| 5 | AI Inference Integration | Spec 0013 (Runtime Host Foundation) | Pluggable inference backend for gameplay/tools use — must not shape Phase 1 rendering abstractions; inference backend not chosen | Candidate — spec not yet drafted; future AI workload, does not shape Phase 1 |
| 6 | UGC Sandbox and Package Model | Candidate 3 (Gameplay SDK) | Sandboxed, capability-scoped user content execution — UGC VM/language not chosen | Candidate — spec not yet drafted; future UGC workload, does not shape Phase 1 |
| 7 | Cross-Session Stable Identity and Asset Catalog | Spec 0015 (Scene Asset & Serialization Foundation) | The broader scope Candidate 2's own prior description named ("Stable GUID/handle schemes, schema versioning and migration") plus a general-purpose, persistent `AssetId`→location Asset Catalog/Registry — both explicitly deferred, disclosed, out of Spec 0015's own narrower scene-asset-bake/load scope; renamed from "Serialization and Stable Identity" when Spec 0015 was drafted 2026-08-23, to make clear what Spec 0015 did *not* cover, not to re-promise a specific future design | Candidate — spec not yet drafted; no consumer (save system, networking, Editor, or a second scene needing cross-scene dependency-location sharing) yet exists to design against |

Image Regression Testing, Asset System Foundation, Runtime Host
Foundation, World/ECS Foundation, and Texture & Sampler Foundation no
longer appear in this table at all — they were promoted to Section A
above as Spec 0011, Spec 0012, Spec 0013, Spec 0014, and Spec 0016
respectively (World/ECS Foundation promoted under the working title
"World / Scene Foundation," per Spec 0014's own drafting), each once a
real spec was drafted for it, per this section's own maintenance rules
below (see Section A for each spec's current status). **Serialization
and Stable Identity is a partial promotion, not a full one:** its scene-asset-bake/load slice was promoted to Spec
0015 ("Scene Asset & Serialization Foundation," `Approved`, Section A
above — see the "Superseded in part again, human-directed 2026-08-23"
note below for the full disclosed scope narrowing), but the remainder
of what that candidate's own prior description named — cross-session
stable identity/schema migration, and a general-purpose Asset Catalog —
was **not** drafted and remains a real, tracked candidate, renamed and
re-listed above as Candidate Order 7 ("Cross-Session Stable Identity and
Asset Catalog") rather than silently dropped. Every remaining
candidate's own `Candidate Order` number and
every cross-reference to a since-promoted item's old number are
renumbered/corrected at each promotion, per this section's own
maintenance rules — this is a mechanical index update only, never a
change to any candidate's own scope or real dependencies.

Items 5–6 (and any future neural-rendering, GPU-driven-rendering, or
world-model candidate) are future AI/UGC/neural workloads: per
[AGENTS.md](../AGENTS.md), they are future phases and must not shape
Phase 1 abstractions ahead of their own approved spec.

### Backlog maintenance rules

- When a candidate item gets a real Spec drafted, assign it a formal
  spec number at that point and move/replace its row into Section A —
  do not pre-assign a number while it is still a backlog entry.
- Status changes in either table come only from a real Spec, Plan, or
  merged PR — never from editing this registry's prose alone.
- This registry is not a Human Review approval and does not authorize
  implementation of anything listed in it.
- An Accepted ADR or an Approved Spec always takes precedence over
  anything stated in this registry.
