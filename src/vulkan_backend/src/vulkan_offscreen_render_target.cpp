#include "vulkan_offscreen_render_target.h"

// Every member of VulkanOffscreenRenderTarget is defined inline in the
// header above -- this translation unit exists only to keep this module's
// one-.cpp-per-class convention uniform and to verify the header compiles
// standalone (include-what-you-use).
